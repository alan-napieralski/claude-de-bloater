---
paths:
  - "**/*"
---

# General conventions

Always reference the semantic colour token `bg-surface-primary` instead of a raw hex value in every CSS file. Use 2-space indentation everywhere. Never commit directly to `main`.
