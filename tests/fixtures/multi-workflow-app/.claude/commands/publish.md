---
description: Publish a reviewed draft, flip draft to false, run the build, confirm the output.
---

Move the named draft from `posts/drafts/` to `posts/`, set its front matter `draft` to `false`, then run `node build.js` and confirm the post appears in `dist/`.
