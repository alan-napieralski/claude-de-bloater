# Purpose

Not a bloat-pattern fixture, there's nothing deliberately wrong here. This exists to give `debloat-verify`'s one-shot canary prompt a genuinely small, realistic, single-purpose project to run against, so the harness's one-shot/brief measurements are validated against something more representative than an empty scratch directory.
